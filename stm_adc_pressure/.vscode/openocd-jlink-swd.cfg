# Use J-Link in SWD mode for STM32F4 targets.
source [find interface/jlink.cfg]
transport select swd

# Start conservatively; lower this further if the link is unstable.
adapter speed 2000

source [find target/stm32f4x.cfg]
